tailwindConfig: ./tailwind.config.js
overrides:
  - files:
      - "*.md"
    options:
      parser: markdown-nocjsp
  - files:
      - "*.mdx"
    options:
      parser: markdown-nocjsp
plugins:
  - prettier-plugin-tailwindcss
